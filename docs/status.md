# AI Scan 功能状态

本页是 AI Scan 唯一功能状态页。官网、用户手册、发布说明和维护文档只引用本页结论，不再另建完整状态表。

状态词含义：

- `可用`：普通用户可以在当前产品里完成操作，结果来自真实本机检测或真实接口。
- `部分可用`：已有真实能力，但还有 UI、接口兼容、登录态或使用边界没有闭环。
- `接入中`：代码或入口存在，但还不能承诺普通用户稳定使用。
- `规划中`：产品目标存在，当前版本不能当作可用功能宣传。

| 模块 | 用户是否可用 | 当前状态 | 验证证据 | 不能承诺的内容 | 下一步 |
| --- | --- | --- | --- | --- | --- |
| 桌面外壳和导航 | 部分可用 | React/Tauri 桌面界面和主要页面结构存在。 | 代码中已有首页、账号、API Key、环境、客户端、修复、安装和专业模式等页面入口。 | 不能仅凭页面入口承诺每个页面的业务动作都已完成。 | 每次发布前验证实际可点击路径和空状态文案。 |
| 本机系统信息 | 可用 | Rust 后端可采集系统、架构、Shell、PATH、部分环境变量和常见工具版本。 | 已有系统资料采集命令，敏感值按规则脱敏。 | 不能承诺已经覆盖所有代理、证书、系统时间和客户端配置问题。 | 扩展代理、证书、时间和 WebView2 检查。 |
| API 连通性快速扫描 | 可用 | 可在 UI 输入 API base URL 和 API Key，检查 DNS、TCP、TLS、HTTP，并在提供 Key 后检查 `/v1/models`。扫描时会显示实时进度条、当前步骤、通过/关注/失败计数和每项耗时。 | 快速扫描命令按网络层级返回结构化结果；流式命令通过 `quick-scan-progress` 事件推送 start、step start、step finish、finish/fail，前端按事件更新结果。 | 不能承诺 chat completions、stream SSE、账号额度和上游模型池一定可用。 | 加入最小 chat completions 和 stream 检查，并把结果接入用户报告。 |
| 诊断报告模型 | 部分可用 | 专业模式可生成脱敏诊断报告，组合系统信息、快速扫描、账号状态、Key 列表和脱敏声明。 | 后端存在报告构建模型，UI 可预览结构化 JSON。 | 不能承诺已经有文件保存对话框、客服上传入口或完整报告复制策略。 | 完成文件导出、报告预览确认和敏感信息二次提示。 |
| 账号登录状态 | 部分可用 | 前端已有邮箱密码登录、2FA 挑战态、登出和账户刷新；后端使用系统安全存储保存会话。 | 命令调用 `/api/v1/auth/login`、`/auth/login/2fa`、`/auth/logout`、`/auth/me`，公开设置接口已 live 返回 JSON。 | 不能承诺 Turnstile 桌面挑战已完成；没有真实用户凭据时不能宣称所有登录场景已验证。 | 用测试账号覆盖成功、密码错误、2FA、过期会话、Turnstile 开启场景。 |
| 余额和额度 | 部分可用 | 账户页会显示 `/auth/me` 返回的余额、并发/角色/状态文本。 | 账号状态模型包含 balance/quota 字段，UI 不显示假余额。 | 不能承诺余额字段单位和套餐到期时间已完全覆盖。 | 用真实登录态验证余额、额度、订阅到期和无额度场景。 |
| 充值入口 | 部分可用 | 账户页会优先使用 public settings 的购买订阅 URL 或余额低通知充值 URL，否则打开官网。 | `settings/public` live 返回 payment/config 类公开字段，UI 提供外部跳转。 | 不能承诺桌面内支付、支付回调或充值后自动刷新余额。 | 确认正式充值 URL、补充值后刷新和支付失败说明。 |
| API Key 列表/创建/删除 | 部分可用 | API Key 页已有真实列表、新建、删除、可用分组选择、删除二次确认和一次性显示新 Key 的 UI。 | Rust 命令调用 `/api/v1/keys` list/create/delete 和 `/api/v1/groups/available`。 | 不能承诺禁用/编辑、IP 白名单、限速、复制策略和所有错误提示已完成。 | 用测试账号验证创建/删除闭环，再补编辑、禁用、复制和限速能力。 |
| API Key 可用性测试 | 部分可用 | 快速扫描可用提供的 Key 请求 `/v1/models`。 | `/v1/models` 检查已纳入 quick scan。 | 不能把 models 成功等同于 chat、stream、余额或所有模型都可用。 | 增加最小 chat 和 stream 测试，明确 401/403/429/5xx 的普通用户解释。 |
| 客户端配置发现 | 部分可用 | 客户端配置页会只读汇总 OPENAI 环境变量、当前扫描输入和常用工具基础状态。 | 前端使用系统资料命令返回的环境变量和工具版本，不写入配置文件。 | 不能承诺已读取 Cursor、VS Code、Cline 等具体配置文件，也不能承诺自动写入。 | 扩展客户端配置文件发现，再做带 diff、备份和确认的写入。 |
| 修复建议中心 | 部分可用 | 修复中心会汇总全盘、系统、网络、安装扫描的真实 Finding，并生成可复制处理建议。 | 页面读取当前扫描结果，不执行系统写入。 | 不能承诺自动修复、静默修改系统设置或修复后一定成功。 | 实现低风险修复计划、用户确认、备份、执行和复检。 |
| 环境安装器 | 部分可用 | 环境安装页会展示 Node.js、npm、Git、curl、Python、Docker、WebView2、VC++ Runtime 等检测状态和安装建议。 | Rust 后端已有命令/注册表检测和 Findings。 | 不能承诺自动下载安装、静默安装或管理员权限处理。 | 逐项增加官方入口和经验证的安装执行器。 |
| 专业模式证据 | 部分可用 | 扫描结果、系统信息、网络检测、运行库检测和脱敏报告可作为结构化证据基础。 | 后端导出报告已包含 quick/system/network/installer/account/apiKeys 分层结果。 | 不能承诺所有原始请求/响应、命令输出、日志和报告文件保存都已完成。 | 补齐证据展开、复制摘要、文件导出和单项复检。 |

维护规则：

1. 新功能上线前必须先更新本页，再同步用户手册、官网、更新日志和 release note。
2. 不允许在其他文档复制完整状态表；需要状态时链接本页。
3. 没有 live 登录验证的账号、余额、充值和 Key 管理能力，只能写为 `接入中` 或 `部分可用`。
4. `/health`、HTML 200、页面入口或 mock 数据不能作为“用户可用”的证据。
